# Client (React)

Run locally:

```bash
cd client
npm install
npm run dev
```

The frontend expects the backend API at `http://localhost:5000` by default. To change, set `VITE_API_URL` in your environment before running, e.g.

```bash
VITE_API_URL=https://your-backend.example.com npm run dev
```
